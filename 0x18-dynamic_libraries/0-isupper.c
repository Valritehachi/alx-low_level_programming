#include "main.h"

/**
* _isupper - checks uppercase letters
* @c: number to check
* Return: 1 if uppercase ,else return 0.
*/

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);

return (0);
}
